# stock pricing
marketstack:
  key: "YOUR_MARKETSTACK_KEY"
  stocks:
    - AAPL
    - MSFT

# No key required
yahoo:
  stocks:
    - GOOG

fixer:
  key: "YOUR_FIXER_KEY"
  pairs:
    - { from: "EUR", to: "USD" }
    - { from: "GBP", to: "USD" }

ledger:
   price_db: "/tmp/prices.db"
